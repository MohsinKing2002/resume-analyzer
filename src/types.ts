export type InputType = {
  label: string;
  placeholder: string;
  id: string;
  name: string;
  type?: string;
  value?: string;
  onChange?: any;
};

export type ButtonType = {
  label: string;
  onClick?: any;
};
